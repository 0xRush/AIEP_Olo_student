__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/
.pytest_cache/
.DS_Store

# Raw and processed data — `make data` reproduces it
data/raw/
data/processed/

# Model weights — large, and `make train` reproduces them.
# metadata.json IS committed: it documents what the artifact contains.
models/*
!models/metadata.json
!models/.gitkeep

# NEVER commit these. Check `git log -p` too, not just the working tree.
.env
*.key
*.pem
